---
classes:
 - 'profile::base'

ntp::servers:
  - 19.us.pool.ntp.org
  - 11.us.pool.ntp.org

timezone::timezone:
  - 'Europe/Lisbon'

users_users:
    root:
        ensure: present
        uid: 0
        gid: root
        comment: 'ESTE ES MI root@%{::clientcert}'
        managehome: true
        shell: '/bin/bash'
        password: '*'
#        ssh_authorized_keys:
#            Clave_root_Juan:
#                ensure: present
#                type: 'ssh-rsa'
#                key: ''